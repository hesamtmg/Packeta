PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=packeta
DB_PASSWORD=packeta
DB_DATABASE=packeta

MONGO_URI=mongodb://localhost:27017/packeta_logs

JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=1h

# The sandbox IPG (ipg-backend/) that PURCHASE initiate/verify calls out to.
IPG_BASE_URL=http://localhost:4000
IPG_API_KEY=sandbox-ipg-key

# Where the customer's browser lands after the IPG redirects back.
FRONTEND_URL=http://localhost:5173
