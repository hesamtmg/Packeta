services:

  mongo:
    image: mongo:7
    restart: unless-stopped
    ports:
      - '27017:27017'
    volumes:
      - mongo_data:/data/db

 
volumes:
  postgres_data:
  mongo_data:
  ipg_postgres_data:
